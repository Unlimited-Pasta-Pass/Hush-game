using UnityEngine;
using UnityEngine.SceneManagement;

public class RespawnDemo : MonoBehaviour
{
    // Start is called before the first frame update
        void Start()
        {
            SceneManager.LoadScene("Demo");
        }
}
